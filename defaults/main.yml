---
# defaults file for ansible-logstash

logstash_user: "root"
logstash_group: "root"

logstash_config_path: /etc/logstash/conf.d

logstash_mysql_slow_install: false
logstash_mysql_slow_path: ""

# plugins
logstash_install_input_beats: false
